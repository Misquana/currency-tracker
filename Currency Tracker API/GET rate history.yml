info:
  name: GET rate history
  type: http

http:
  method: GET
  url: http://localhost:3000/api/rates/USD/history?days=5
  params:
    - name: days
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
