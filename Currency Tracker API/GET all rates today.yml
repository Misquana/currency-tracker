info:
  name: GET all rates today
  type: http

http:
  method: GET
  url: http://localhost:3000/api/rates/today
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
