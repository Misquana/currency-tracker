info:
  name: GET single rate
  type: http

http:
  method: GET
  url: http://localhost:3000/api/rates/USD
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
