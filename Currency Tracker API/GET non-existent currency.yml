info:
  name: GET non-existent currency
  type: http

http:
  method: GET
  url: http://localhost:3000/api/rates/XXX
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
