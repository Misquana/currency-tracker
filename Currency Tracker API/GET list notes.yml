info:
  name: GET list notes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/notes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
